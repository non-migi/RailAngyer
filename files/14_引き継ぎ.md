# RailAngyer 引き継ぎ資料

> 作成日: 2026-07-28。ここまでの作業を別のエージェント／人が引き継ぐための文書。
> **仕様の正は `01`〜`13` の各文書**で、ここはそれらへの入口と、
> 文書に書ききれていない「実際に踏んだ落とし穴」をまとめたもの。

---

## 1. 30秒でわかる現状

サイコロで駅を巡る徒歩すごろく（身内向け / iOS）。**フェーズ1〜4の「作るもの」は一通り揃っている。**

| 層 | 状態 |
|---|---|
| iOSアプリ（SwiftUI + SwiftData） | ホーム・明示開始（スタート駅も1駅目扱い）・複数旅記録・位置判定・写真（駅の詳細を開かずに見られるギャラリーつき）・同期（同時ロールの自動追随）・予定（詳細設定にルールを集約、終了後は自動で畳む）・設定の整理（言語・募金・使い方とお約束・アプリ情報の4項目）・連続ペース色・区間の地図と歩く目安・予定の外部共有・多言語（8言語、翻訳は途中）・応援課金まで実装 |
| API（ASP.NET Core / net10.0） | 参加・ミッション（既定で全員に見える）・進行・写真SAS・予定（詳細設定）まで実装。**2026-08-11 01:29 JSTに統合版をApp Serviceへ再配置し、`/health` `/health/db` `/courses`（14件）で疎通確認済み** |
| Azure | SQL Database（Japan East）＋ Storage ＋ App Service（**Japan West / F1無料**） |
| コース | 日本7本（南北線16 / 東西線19 / 東豊線14 / 山手線30 / 札幌市電24 / 阪急京都本線28 / 大阪環状線19）＋海外7本（ベルリンRingbahn27 / ロンドンサークル線27 / モスクワ環状線12 / モスクワ大環状線29 / 北京地下鉄10号線45 / ニューヨーク7号線22 / ロサンゼルスEライン29）。**全14本341駅**。札幌3路線と山手線はN02-22、他はOSM |

- 本番API: `https://railangyer.azurewebsites.net`（`/health` `/health/db` で疎通確認）
- テスト: 統合（2026-08-11）後の件数は §10 の「今回の作業」を参照
- TestFlight: `1.0.0 (27)`（予定の作り直し・設定整理・多言語・海外コース・応援課金の統合版）を
  2026-08-11 01:33 JSTにアップロード成功（処理中）。アーカイブは `/tmp/RailAngyerApp-1.0.0-27.xcarchive`。
  別ライン（worktree-plan-map-share）はこのビルドで `main` に統合し、以後は分けない
- APIのルーム越権修正（2026-08-10）は同日20:24 JSTにApp Serviceへ配置済み（`/health` `/health/db` で確認）。
  統合版の再配置は上記の通り2026-08-11 01:29 JST

**残っているのは実機での通し確認と実地テスト**（座標の実測、テンポ、電池）。
これは歩かないと確かめられないので、コード側の作業ではない。

---

## 2. 最初に読むもの（この順で）

| # | 文書 | 何が書いてあるか |
|---|---|---|
| 1 | `04_ロードマップ.md` | フェーズごとの完了状況。**まずここで全体像をつかむ** |
| 2 | `11_API設計.md` | APIの方針・エンドポイント・冪等性・**実機で踏んだ不具合の記録** |
| 3 | `10_アプリ設計.md` | SwiftDataモデル・状態管理・位置判定・ふりかえりの集計 |
| 4 | `03_Azure構成と料金.md` | 作ったリソース・無料枠・**F1がJapan Westにある理由** |
| 5 | `05_フロー仕様.html` / `08_画面仕様.html` | 状態遷移と画面の一覧（ブラウザで開く） |

---

## 3. 開発環境と、よく使うコマンド

### 前提

`Xcode`（iOS 26.5 SDK） / `xcodegen` / `dotnet`（net10.0） / `az` / `sqlcmd`。
いずれも導入済みで、`az login` と `az account`（Azure subscription 1）も通っている。

### ビルドとテスト

```bash
# ルール計算と駅マスタ（GPS・DB非依存。いちばん速い）
swift test --package-path RailAngyerCore

# API（SQLiteのインメモリDBで実際のHTTPを通す。Azureには触らない）
dotnet test RailAngyerApi.Tests

# アプリ（ユニット＋UIテスト）
xcodegen generate --spec RailAngyerApp/project.yml     # ファイルを足したら必ず実行
tools/prepare-simulator.sh "iPhone 17 Pro"             # 位置情報を許可（新しい環境では必須）
xcodebuild -project RailAngyerApp/RailAngyerApp.xcodeproj -scheme RailAngyerApp \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test
```

> ⚠️ **UIテストの前に `tools/prepare-simulator.sh` を打つ。**
> 飛ばすと位置情報の許可ダイアログが操作を塞ぎ、
> `ArrivalUITests` の自動到着（`testWalkingIntoRadiusTriggersArrival`）だけが落ちる。
> **アプリの不具合ではない。** 手動到着のテストは通るので、片方だけ落ちたらまずこれを疑う。

> ⚠️ **`.xcodeproj` は `project.yml` から生成する。手で編集しない。**
> `Sources/` や `Tests/` にファイルを追加したら `xcodegen generate` を忘れると
> 「ビルドは通るのに新しいファイルが入っていない」状態になる。

### 画面を実際に見る

```bash
xcrun simctl boot "iPhone 17 Pro"
APP=$(find ~/Library/Developer/Xcode/DerivedData -name "RailAngyerApp.app" -path "*Debug-iphonesimulator*" | head -1)
xcrun simctl install booted "$APP" && xcrun simctl launch booted com.non-migi.RailAngyerApp
xcrun simctl io booted screenshot /tmp/shot.png
```

UIテストの中で撮ったスクリーンショットを取り出す場合:

```bash
xcodebuild ... -resultBundlePath /tmp/result.xcresult test
xcrun xcresulttool export attachments --path /tmp/result.xcresult --output-path /tmp/att --test-id "<テストID>"
```

### デプロイ

```bash
dotnet publish RailAngyerApi -c Release -o /tmp/publish
(cd /tmp/publish && zip -qr ../app.zip .)
az webapp deploy -g RailAngyer -n railangyer --src-path /tmp/app.zip --type zip
```

### DBに繋ぐ

```bash
# 管理者（スキーマ変更・調査）。パスワード不要
sqlcmd -S railangyer.database.windows.net -d RailAngyer --authentication-method ActiveDirectoryAzCli

# 作業場所を変えたらファイアウォールに自分のIPを足す
ip=$(curl -s ifconfig.me)
az sql server firewall-rule create -g RailAngyer -s railangyer \
  -n "mac-$(date +%Y%m%d-%H%M)" --start-ip-address $ip --end-ip-address $ip
```

---

## 4. 設計の芯（ここを外すと壊れる）

| 原則 | 具体 |
|---|---|
| **ローカルが正、サーバーは共有の場** | プレイは送信キューの状態に依存しない。圏外でも最後まで遊べる |
| **書き込みはすべて冪等な PUT** | 主キーはクライアント生成のUUID。再送しても二重にならない |
| **順序は時刻で決める** | 現在位置は `TurnNo` ではなく `RolledAt` の順で導く（採番衝突を実害にしない） |
| **他人のお題はAPIが伏せる** | クライアントで隠すだけでは通信を覗けば見える |
| **リクエストの `MemberId` を信用しない** | 誰がやったかは必ずトークンから引く |
| **写真の実体はAPIを通さない** | 単一Blob限定・短命のSASで端末とBlobを直結（F1の帯域を使わない） |
| **テーブルの正は `06_schema.sql`** | EF Core はその形に合わせるだけ。マイグレーションでスキーマを作らない |
| **駅マスタの正は `RailAngyerCore/Resources/*.json`** | 投入SQL（`13_courses.sql`）はJSONから生成する。手で書き写さない |

---

## 5. 実際に踏んだ落とし穴（同じ穴を掘らないために）

| 症状 | 原因 | 対処 |
|---|---|---|
| `POST /rooms` が 500 | `MissionSet.CreatedBy` と `Member.MissionSetId` が**循環参照**。1回の `SaveChanges` ではどちらのINSERTが先でもFK違反（547） | ルーム→メンバー→紐付けの**3段階で保存**する |
| その不具合をテストが見逃した | `CreatedBy` のFKがEFのモデルに無く、テスト用SQLiteに制約が作られていなかった。さらに**自前で開いたSQLite接続にはEFが `PRAGMA foreign_keys=ON` を送らない** | モデルにFKを追加し、`ApiFactory` でPRAGMAを送る |
| 空のお題が保存できてしまう | SwiftDataは**保存済みオブジェクトに関係を張った時点で**コンテキストに入れる。「作ってから弾く」では手遅れ | 値だけで判定する `Mission.validationError(content:...)` を関係を張る前に呼ぶ |
| App Service で設定を変えたのに読まれない | アプリケーション設定の変更で自動再起動しないことがある | `az webapp restart` を打つ |
| TestFlightアップロードが `Failed to Use Accounts` / `No Accounts` | XcodeのApple IDセッションが切れると `-allowProvisioningUpdates` だけでは署名・送信できない | `xcodebuild -exportArchive` に `-authenticationKeyPath ~/private_keys/AuthKey_<キーID>.p8 -authenticationKeyID <キーID> -authenticationKeyIssuerID <Issuer ID>` を足す（`15_TestFlight準備.md` §5） |
| アップロードが「bundle version must be higher than ‘25’」で弾かれる | App Store Connect上の実際のビルド番号は文書の通し番号（1〜5）より大きい。**正はApp Store Connect側** | エラーが示す番号+1を `project.yml` の `CURRENT_PROJECT_VERSION` に入れて作り直す |
| ローカル実行で接続文字列が読まれない | user-secrets は **Development でしか読まれない** | `DOTNET_ENVIRONMENT=Development dotnet run` |
| `az appservice plan create --sku F1 --location japaneast` が失敗 | Japan East のVM枠が0 | **Japan West** に置いた（DBはJapan Eastのまま。国内同士で往復は数ms） |
| `sqlcmd` の1回目が必ず失敗 | サーバーレスDBの自動一時停止からの復帰 | 再試行する。アプリ側も起動時に `/health` を投げて起こす |
| **起動直後に落ちる**（`EXC_BREAKPOINT` / `_InvalidFutureBackingData`） | 消したお題を `Turn.selectedMission` が掴んだまま残り、`selectedMission?.id` を読んだ瞬間に落ちる。**SwiftData は消えた行の属性を読むと fatalError で止まる**（catch できない） | 消す前に参照を外す。読むときは属性ではなく `persistentModelID` で突き合わせる。起動時に `repairDanglingReferences()` で古い参照を掃除する |
| 日時のパースに失敗しうる | サーバーは `DATETIME2` をそのまま返すため **末尾に `Z` が付かない**ことがある | `ApiDate.parse` が両方受ける。テストは `Tests/Fixtures/*.json`（実応答）で固定 |
| **UIテストの自動到着だけが落ちる**（`半径に入っても到着にならない`） | シミュレータに位置情報の許可が無く、ダイアログが操作を塞ぐ。手動到着のテストは通るので不具合に見える | `tools/prepare-simulator.sh` を**先に**打つ。ダイアログをテスト内で閉じる方法は取りこぼしがあったため、事前付与の方式にしてある |
| 許可を付与したのに、まだ自動到着だけが落ちる | `simctl privacy grant` の **`location-always` だけでは足りない**。消したばかりのシミュレータでは測位が始まらない。アプリは使用中→常に許可の2段で求めるため、1段目の `location` が要る | **両方を付与する。** `prepare-simulator.sh` は2つ打つようにしてある |
| UIテストが軒並み `Test crashed with signal kill` / `Restarting after unexpected exit` になる | 同じシミュレータで重い実行を何度も続けると詰まる（地図を描く画面が多い）。**アプリは落ちていない**（手で起動すれば動く） | `xcrun simctl shutdown` → `erase` → `boot` → `prepare-simulator.sh` で作り直す。落ちた顔ぶれを**変更前のコミットでも**流して、同じなら環境side |
| **UIテストが単独では通るのに、まとめて流すと落ちる**（`手動到着のボタンが見つからない`） | シミュレータの現在地は**テストをまたいで残る**。位置を動かすテストの後に走ると、出発した瞬間に目的地の圏内にいて自動到着し、手動到着のボタンが消える | 手動到着を使うテストは `setUp` で現在地を麻生に戻す（`ResetJourneyUITests` / `TimingUITests`） |
| コースを1本足したら、関係のないテストが落ちた | 同梱コースの**名前と本数をテストに書き写していた**（`ModelsTests` / `CourseSelectionTests`） | 数と名前は `StationMaster.all()` から引いて突き合わせる。書き写しは必ず腐る |
| 駅間が4kmを超える路線を弾いてしまった | 駅間の上限を市街地の地下鉄に合わせて4kmにしていた | 郊外の路線には長い駅間が実在する（阪急京都本線 高槻市〜上牧 約4.3km）。上限は徒歩1時間ぶん＝5kmに緩めた |
| **予定を保存した直後に落ちる**（`EXC_BAD_ACCESS` / `SwiftData.DefaultStore.FutureCache.pruneValues` / 落ちたのは**メインスレッドではない**） | `SyncService` に `@MainActor` が無く、`await` のたびに別スレッドへ移っていた。取り込みの `ModelContext.save()` が、画面側の読み出しと同時に走って壊れる。**SwiftData のコンテキストはスレッドをまたげない** | `SyncService` / `GameSessionStore` / `SyncQueue` を `@MainActor` にする。**外さない。** 通信は `URLSession` が別スレッドで行うので、待っている間に画面は止まらない |
| その不具合が長く見つからなかった | 競合なので**運が良ければ通ってしまう**。しかも `pullSchedules` にテストが1件も無かった | 取り込みは中身のテストを必ず置く（`SyncServiceTests` の「予定の取り込み」5件）。**落ちたスタックの一番上がアプリの外（SwiftData / libswiftCore）なら、まずスレッドを疑う** |
| **予定が立てられない**（保存が押せる状態にならない） | 地図（170pt）を先に挟んだせいで、**名前の入力欄が画面の外へ出た**（y=855 / 画面高874）。保存は名前が空だと押せないので、押せない理由が見えない | 新規の予定は**名前を最初から入れる**（「南北線を歩く」）。名前のセクションを地図より前に置く。空のときは赤字で理由を出す。**入力より下に飾りを置かない** |
| **テストを流すたびに、本番の利用状況が汚れていた** | 測定を止める判定が `RAILANGYER_IN_MEMORY` だけだった。それを渡すのは**UIテスト**で、**ユニットテストはアプリを起動したまま走る**ため素通りしていた | `NSClassFromString("XCTestCase")` でも見る。**「テスト用の環境変数」だけを頼りにしない** |
| **2台で遊ぶと、片方が出発した駅から動かない** | 取り込み（`apply(_ state:)`）が**進行中のターンと訪問だけ**を見ており、**終わったターンを一切取り込んでいなかった**。現在地は終わったターンから計算するので、相手が進んでも何も伝わらない。しかも既にあるターンを**更新せず**、進行中→完了の変化も反映されなかった | `/state` に `completedTurns` を足し、取り込みを**upsert**にした。**「足すだけ」の取り込みは、状態が変わるものには使えない** |
| **「この予定を共有する」を押しても何も起きない** | `ShareLink` に `.simultaneousGesture(TapGesture())` を足していた（記録を取るため）。**ジェスチャーが `ShareLink` の反応を奪う** | 押した瞬間に何かしたいなら `ShareLink` を使わない。ボタン＋`ShareSheet`（`UIActivityViewController`）にする |
| そのとき書いたテストが**通ってしまった** | 「共有シートが出たか」を `app.collectionViews.firstMatch` で見ていたが、**予定の一覧そのものが CollectionView** だった | シート特有の要素（`ActivityListView`、「コピー」）で見る。**通ったからといって直っているとは限らない。画面を撮って目で確かめる** |
| **配ったアプリのアイコンが古いままだった** | アイコンを直したあと、**配布用のアーカイブを作り直していなかった**。シミュレータのビルドは通るので気づかない | `.ipa` を展開してアイコンの色を確かめてから配る（Apple最適化PNGなので `pngcrush -revert-iphone-optimizations` を挟む）。**アセットを変えたらアーカイブから作り直す** |
| **コースの行を押しても何も起きない** | `LabeledContent` を `Button` に入れて `.buttonStyle(.plain)` を付けたら、タップがボタンに届かなくなった。**アクセシビリティ上は `hittable: true`** なので、テストからは押せているように見える | 行は素の `HStack` ＋ `.contentShape(Rectangle())` にする。**`app.debugDescription` を撮って、押した後に画面が変わったかで判断する** |
| **路線を選んでも入力画面へ戻らない** | 3段たどった先で `dismiss()` を呼んでも、`NavigationLink` で押し込んだ画面までは戻らない。選んだあとピッカーの中に取り残される | コース選択は**独立したシート**にし、自前の `NavigationStack` を持たせる。何段たどっていても `dismiss()` ひとつで確実に閉じる |
| お題を「いつでも見える」にしたら、他人のお題が全部自分の作ったものになった | 取り込みで `mission.member` に一律 `me` を入れていた。伏せる設定では自分のぶんしか返らないので、それで足りていた | **書き手（`createdByName`）で突き合わせる。** 見え方を変えると返る顔ぶれが変わることを忘れない |
| お楽しみへ戻したのに、他人のお題が見えたまま | サーバーは返さなくなるが、**端末に取り込んだぶんは残る** | 戻したときに、自分以外が書いたお題を端末から消す（`updateMissionVisibility`） |
| **ある日から突然テストが落ちる**（`立てた予定はその場で一覧に出る`） | テストが**固定の日時**を書いていた（`Date(timeIntervalSince1970: 1_786_000_000)`）。`nextSchedule` は先の予定しか返さないので、その日を過ぎた瞬間に落ちる。**コードは何も変えていないのに落ちる** | 先か後かを見るテストは `Date().addingTimeInterval(...)` で書く。**日時を固定してよいのは、書式を確かめるときだけ**（`ScheduleShareTests`） |
| 濃い緑の上の文字が読めない | 地に `LaunchBackground`（RGB 0.02/0.38/0.18 の**濃い緑**）を敷き、その上に `.secondary` の灰文字と `Theme.line` の緑文字を置いていた。コメントには「淡い紙色」とあったが実物は濃緑 | **地は淡く保つ**（`Theme.surface` / `Theme.canvas`）。乗換案内の類はどれも地を白か淡い灰にして、色は路線と主ボタンだけに使う。起動画面だけは濃くてよい（ロゴを白抜きで置く面なので） |
| **日付が「Aug 2, 2026 at 9:00」と英語で出る** | `DEVELOPMENT_LANGUAGE` が既定の `en` のままで、**アプリが英語のアプリとして名乗っていた**。端末が日本語でも `Date.formatted()` はアプリの言語に従う | `project.yml` で `DEVELOPMENT_LANGUAGE: ja` と `CFBundleLocalizations: [ja]` を宣言する。**画面ごとに `.environment(\.locale)` を貼って回らない** |
| サイコロが斜めのまま止まって見える | 静止姿勢に z（横倒し）を 0.05 入れていた。立体に見せるつもりが「まだ転がり切っていない」と読める | **z は 0 にする。** 傾けるなら x/y だけ、それも浅く |
| お題を書く画面に、区間の外の駅まで出ていた | ホームから開いたとき（予定を渡さないとき）に**コースの全駅**を並べていた | 予定が無いときは `store.stationsInOrder`（＝いま遊んでいる区間）を使う。区間外の駅に書いても永久に引かれない |
| `xcodebuild -exportArchive` が `error: exportArchive Failed to Use Accounts` で落ちる | Xcode に入れた Apple ID のセッションが切れた。**アーカイブは通るのに送信だけ落ちる**ので、ビルドの不具合に見える | サインインし直さなくてよい。`-authenticationKeyPath` / `-authenticationKeyID` / `-authenticationKeyIssuerID` に**クラッシュ報告と同じ鍵**を渡す（`15_TestFlight準備.md` §5） |
| **撮った写真がどこにも出ない**（地図・一覧・帯すべて空。端末の問題に見える） | `URL.path()` の既定は**パーセントエンコード**。保存先は「Application Support」＝**空白を含む**ので `Application%20Support` になり、`UIImage(contentsOfFile:)` も `FileManager` もファイルを見つけられない。**保存は成功しているので気づきにくい** | URLから直接 `Data` を読む。文字列にするときは `path(percentEncoded: false)`。**保存と読み出しを往復させるテストを置く**（`PhotoStoreTests`） |
| 写真の容量が想定の9倍になる | `UIGraphicsImageRenderer` は**端末の倍率（3倍）で描く**。1600ptのつもりが4800ピクセルで保存される | `UIGraphicsImageRendererFormat` の `scale` を1に固定する |
| **一周で「途中の駅」が大量に出る**（札幌市電で5駅のはずが20駅近く） | 経路を**駅の通し番号**で計算していた。一周では番号が一巡して元に戻るため、`path(24, 4)` のように逆向きへ路線をほぼ一周ぶん並べる | **位置（`fromPosition` / `landingPosition`）で数える。** 計算は画面から `GameSessionStore.passingPositions(of:)` へ寄せた。**一周まわりは番号と位置を必ず区別する** |
| **一周の予定で、お題を書く画面に地図が出ない**（札幌市電） | 一周は**スタートとゴールが同じ駅**。それを範囲として扱うと `1...1` になり、駅が1つしか取れない。地図は2駅以上でしか出ない | `isLap` を見て、**一周ならコース全体**を返す。予定の一覧・共有・お題の3か所とも同じ罠がある。**一周を足したら、区間を範囲で絞っている場所を全部見直す** |
| **駅の並び順が方位で並べると狂う** | 環状線を「中心からの方位角」で並べたら、テムズ川沿いで順序が入れ替わり、隣どうしでない駅が3.2km離れて並んだ | **路線リレーションの member 順を使う**（`rel[...]->.r; .r out body; node(r.r:"stop"); out body;`）。方位での並べ替えは円に近い路線でしか通用しない |
| Overpass が `HTTP 406` / `504` を返す | 406は **User-Agent が無い**。504は範囲や正規表現が重すぎる | UAを付ける。bbox を絞る。名前の正規表現を大きな範囲に投げない（**`S41` はスイスにもベルギーにもある**） |
| 地図に出したお題の文字が読めない | 縮尺に関わらず**全駅に出していた**。路線全体が入る画面では16〜30個の吹き出しが、駅名とも経路の線とも重なる | **寄ったときだけ出す**（緯度0.02度＝約2.2kmより狭いとき）。`onMapCameraChange` で判断する。色で塗らず下地を透かすと線の上でも読める |
| 盤面へ戻ったとき、戻り口が「サイコロを振る」に被った | 戻り口を `overlay` で**浮かせて重ねた**。しかも進行中のサイコロは押しても何も起きない飾りだった | 重ねない。**盤面の操作そのものを差し替える**（進行中は「◯◯へ歩く」に変える） |
| 山手線の「東京スタート・東京ゴール」が**予定では作れない** | 予定の保存が `startOrder == goalOrder` を一律に弾いていた。ルール設定には「一周する」があるのに、予定には無かった | `Schedule.isLap` を持たせ、**環状線のときだけ**同じ駅を許す。まわる向きも予定に持たせて、始めるときに移す |
| 歩いている間ずっと全体マップが見えない | `TurnFlowView` を `isPresented: .constant(...)` で出しており、**閉じる手段が無かった**。次の駅しか見えず「あとどれくらいか」が分からない | 画面の上に「次の駅／全体マップ」の切り替えを置き、右上から盤面へ抜けられるようにした。抜けている間は戻る口を盤面に出し、次に振ったら自動で前に出す |
| **UIテスト10件のうち8件が `Test crashed with signal kill.` で落ちる**（アサーション失敗ではない。アプリのクラッシュログも残らない） | 画面を持たないセッション（バックグラウンド実行）でUIテストを流すと、テストランナー側がSIGKILLされる。**`main` の状態でも同じ8件が同じ理由で落ちる**ので、コードの退行ではない | 切り分けは**変更前のコミットで同じUIテストを流して比べる**のが確実（同じ顔ぶれが落ちれば環境）。UIテストは対話セッションで流す。ユニットテストは影響を受けない |
| **お題の「達成した／できなかった」を押しても、永久に「進行中」のまま**（ビルド28〜30で出荷済み） | `appliedEffectType` は `EffectType?`。`= .none` と書くと Swift は **`Optional.none`（＝nil）**として解決する。`MissionOutcome` は `appliedEffectType == nil` を「進行中」と読むので、判定を記録したつもりが「未判定」を書いていた。**コンパイラは曖昧警告で正しく指摘していた**が、「担当外のコードの警告」として流されていた | Optional な変数へ入れるときは **`EffectType.none` と型を明示**する。**警告は担当範囲外でも中身を読む**。なお `enqueueTurn` は `appliedEffectType?.rawValue` を送るので、この不具合は**仲間の端末にも判定が届かない**という実害を伴っていた |
| テストは全件通っているのに `** TEST FAILED **` になる（偽レッド） | ログにテストの ✘ は無く、XCUI の `Error getting main window ... kAXError -25218` が出る。**複数の `xcodebuild` が同じシミュレータを取り合っている**（担当が並行して回すと起きる） | UIテストとユニットテストで**別のシミュレータを使う**か、**直列に回す**。失敗の中身を読み、テストの ✘ が1件も無ければまずこれを疑う |
| **テストが「成功」と出るのに、実は1件も走っていない**（偽グリーン） | `-only-testing:スイート/関数名` の形は、`@Test("日本語の表示名")` が付いた関数に**一致しない**。しかも一致ゼロのとき `xcodebuild` は警告を出さず**成功を返す**。「直したので1件だけ流して確かめた」がそのまま嘘になる | 指定は**スイート単位**（`-only-testing:RailAngyerAppTests/ModerationTests`）にする。そのうえで `Test run with N tests` の**件数を必ず読む**。直したテストは**わざと壊して落ちること**まで見る |
| **退室が本番でだけ 400 `invalid_reference` になる**（テストは全部通る） | アプリ用ロールに `dbo.Member` の **DELETE 権限が無かった**（最小権限で SELECT/INSERT/UPDATE だけ与えていた）。実体は `DELETE permission was denied on the object 'Member'`(229) で、`Program.cs` の例外ハンドラが `DbUpdateException` をまとめて 400 に落とすため**理由が隠れる** | **テーブルを消す操作を足したら `09_アプリ用ユーザー.sql` の権限も足す**（`29_migration_v8`）。テストは SQLite で権限の仕組みが無いので**絶対に検出できない**。理由が隠れたときは `az webapp log tail` を流しながら再現すると実体が読める |
| **退室が本番でだけ必ず 500 になる**（テストは全部通る） | `Program.cs` の `EnableRetryOnFailure` により SQL Server では再試行実行ストラテジが効いており、**ユーザー開始のトランザクションを直接張ると `InvalidOperationException`**。テストは SQLite で非再試行なので**素通しする** | トランザクションは `db.Database.CreateExecutionStrategy().ExecuteAsync(...)` の**中で**張る（`MemberEndpoints.RemoveMemberDataAsync`）。この類はユニットテストで防げないので、**デプロイ後に使い捨てルームで実地確認**する |
| サーバー側の削除が途中で止まり、中途半端な状態が残る | 削除の `SaveChangesAsync(ct)` の `ct` は `HttpContext.RequestAborted`。アプリは90秒で諦める（`ApiClient.coldStartTimeout`）ので、**電波の悪い場所で退室すると接続が切れて削除が中断する**。異常系ではなく普通に起きる | 後戻りできない点から先は `CancellationToken.None` で通し、トランザクションで括る。**Blob 削除だけは外に置く**（消したら戻せないため）。結果、残りうる中間状態を1種類に限定する |
| アプリが「削除できなかった」と言うのに、サーバーでは消えている | 上の対処でサーバーは接続断後も**完走する**ようになった。アプリ側はタイムアウトで失敗と判断するため、両者の認識がずれる | 削除のやり直しでは **401/404 を「もう消えている＝成功」として扱う**（`ApiError.isAlreadyGone`）。失敗として数え続けると、成功しているのに「メールで依頼を」という**嘘の案内**に達する |

---

## 6. 写真・時間・ペースの追加要望（完了）

利用者から出た次の4要望は、集計・画面反映・テスト・シミュレータ確認まで完了した。

### 要望の全文

1. 地図で、写真を撮った駅がピンで分かるようにする
2. 合計時間・移動時間・ミッション時間を記録し、各場面で見られるようにする
3. 「何分/km」のペースを出す
4. 道案内は点線、実際に通ったところは実線にし、**速ければ青系・普通は緑・遅ければ赤**で色分けする

### 実装したもの

| 対象 | 内容 |
|---|---|
| `RailAngyerCore/Sources/RailAngyerCore/Pace.swift` | `Geo.distanceMeters`（ハーバサイン）、`Pace`（分/km・時速・表示）、`PaceCategory`（速い/ふつう/ゆっくり）、`DurationText`。**テスト7件（`PaceTests`）付き** |
| `RailAngyerApp/Sources/Game/WalkTiming.swift` | 区間ごとの記録（`Leg`）、ターンの内訳（`TurnBreakdown`：移動/ミッション/合計）、全体集計（`Total`）。**新しく保存するデータは無い**。既存の `rolledAt` / `arrivedAt` / `completedAt` から後で数え直す |
| `GameSessionStore` | `timing` / `legs` / `currentTurnBreakdown` / `legs(arrivingAt:)` / `photographedOrders` を追加 |
| `BoardMapView` / `BoardView` | 写真駅にカメラのピン、未通過は点線、通過済みは実線でペース色分け（青/緑/赤）。凡例と全体時間・平均ペースつき |
| `StationDetailView` | 訪問ごとに前の駅、区間時間、分/km、効果移動かを表示 |
| `TurnFlowView` | 今回の合計・移動時間とペースを1秒ごとに更新 |
| `JourneySummary` / `JourneySummaryView` | 合計・移動・ミッション・その他と平均ペースを表示。共有画像にも同じカードを使用 |
| `WalkTimingTests` | 区間分割、ミッション時間、効果移動、進行中、時刻不整合の5件 |
| `TimingUITests` | 1ターンを通して盤面・駅詳細・ふりかえりを検証し、3画面のスクリーンショットを保存 |

**速さの区切り**は `PaceCategory` に定義してある。10分/km未満=速い、10〜14=ふつう、14超=ゆっくり
（徒歩の平均は約12分/km）。駅間の**直線距離**で測るので、実際のペースよりやや遅め（数字が大きめ）に出る。

シミュレータでは、地図の凡例が下部ボタンと重ならず、駅詳細とふりかえりの値も
欠けずに収まることを確認済み。色だけに頼らず、凡例と分/kmを併記している。

---

## 7. 場所から選ぶコースと、歩く前に分かる道のり（完了）

札幌・東京の外へ出るにあたって、**コースの選び方**と**歩く前に道のりが分かること**を足した。
設計の詳細は `10_アプリ設計.md` の「コースを場所からたどる」以降にある。

| 対象 | 内容 |
|---|---|
| `RailAngyerCore/Resources/hankyu_kyoto.json` | 阪急京都本線28駅（大阪梅田〜京都河原町）。座標は OpenStreetMap（ODbL） |
| `CourseRef.country` / `countryCode` / `regions` | 駅マスタに国と都道府県を持たせた。**書いていない定義は日本として読む** |
| `RailAngyerCore/WalkEstimate.swift` | `GeoPoint` と `WalkEstimator`。直線距離×1.3÷時速5kmで区間の距離と時間を見積もる |
| `CourseDirectory` / `CoursePickerView` | 国 → 都道府県 → 路線の3段。**県をまたぐ路線は通る両県から選べる** |
| `CourseSectionMapView` / `CourseSectionSummaryView` | 区間の地図と目安。予定・お題・ターン中の3か所で同じ部品を使う |
| `ScheduleShare` | 予定を1つの文にして `ShareLink` で外へ出す。集合場所は地図のリンク |
| テスト | コア `WalkEstimateTests` 9件・`StationMasterTests` 追加4件、アプリ `CourseDirectoryTests` 9件・`ScheduleShareTests` 11件 |

**決めたこと（外すと作り直しになる）**

- **経路探索はしない。** 予定の段で必要なのは「半日で終わるのか」の判断だけ。
  区間の駅数ぶん通信を叩くことになり、当日の道取りでも変わる
- **目安にお題と休憩の時間は含めない。** 出目とお題の中身に依存して当たらなくなる
- **国を最上位に置く。** いまは日本だけだが、日本の外が増えても画面を作り直さない
- **共有の文に招待コードと内部のIDは載せない。** 誘う相手と、ルームに入れる相手は別の話
- 一覧の行に置く地図は**操作を切る**。地図が指を奪うと一覧をスクロールできない

> `files/19_courses_v3.sql`（阪急京都本線のDB投入）は **まだ適用していない**。
> アプリは同梱JSONから読むので動作には要らないが、
> サーバー側のコース一覧を合わせるなら打つ。何度実行しても安全。

---

## 8. お題の見え方・共有からの参加・予定から始める（完了）

| 対象 | 内容 |
|---|---|
| `MissionVisibility` / `MissionSet.missionVisibility` | 当日までのお楽しみか、いつでも見えるか。**ルームの取り決め。既定は「いつでも見える」**（2026-08-11に変更。§10.3） |
| `MissionEndpoints` の `GET /missions` | **サーバーが返す顔ぶれを切り替える。** クライアントで隠すだけでは通信を覗けば見える |
| `files/20_migration_v5_mission_visibility.sql` | `MissionSet.MissionVisibility` を足す（**2026-08-02 適用済み**） |
| `InviteLink` | `railangyer://join?code=…`。共有の文からアプリを開いて、そのまま参加できる |
| `InviteAcceptView` | 開いたリンクの確認。**踏んだだけで勝手に参加させない**（表示名が要る） |
| `StartFromScheduleView` / `applySchedule` | 旅を始めるとき、立ててある予定からルールを移して始める |
| `MapPlaceDetailView` | 地図の施設を押すと分類・住所・電話が出る。`MapFeature` で受ける |
| `HowToPlayView` / `DocumentView` | 遊び方（初回に1度）と、利用規約・プライバシーポリシー（同梱） |

**決めたこと（外すと作り直しになる）**

- **お題を伏せるかどうかは、サーバーが返す内容で決める。** 画面で隠すだけにしない
- **見え方を変えると、取り込むお題の顔ぶれが変わる。** 書き手で突き合わせ、
  伏せる設定へ戻したら端末に残る他人のお題を消す
- **共有は地図ではなくアプリへ誘う。** 見てほしいのは集合場所ではなく、参加そのもの。
  ただし**招待コードは文字でも残す**（アプリを入れていない相手にリンクは効かない）
- **リンクは他人が作れる。** 読む側で scheme と host を確かめ、知らない形は無視する
- **予定から始めるのは、歩き始める前だけ**（T-06）。区間外を指す記録が残ると壊れる
- `MapSelection` は **iOS 18 から**。iOS 17 を切れないので `MapFeature` を使う

> DB移行とAPIの再配置は **2026-08-02 に済ませた**。本番で動作も確かめてある
> （伏せる設定では自分のぶんだけ、いつでも見える設定では書き手つきで全員ぶんが返る）。
>
> ⚠️ **既定値は2026-08-11に「当日までのお楽しみ」から「いつでも見える」へ変更した**
> （ユーザー要望）。`26_migration_v6_mission_visibility_default.sql` で既存3ルームの値も
> 揃え、`MissionSet` / `Schedule` それぞれのサーバー既定・アプリ既定も合わせて直してある（§10.3）。

---

## 9. 歩いた跡・地図の作り込み・写真（完了）

| 対象 | 内容 |
|---|---|
| `TrackPoint`（モデル） | 歩いている間のGPSを間引いて残す。**サーバーへは送らない**（生の座標を端末外に出さない約束） |
| `RailAngyerCore/TrackSegments.swift` | 跡を**50mごとに刻んで**区切りごとの速さを出す。テスト11件 |
| `CourseRouteProvider` | これから歩く線を**道路沿い**にする。手前の区間から少しずつ、取れなければ直線のまま |
| `JourneyTrackMapView` | ふりかえりの全体地図。跡・通った駅・撮った写真を1枚に |
| `PhotoGalleryView` / `PhotoViewerView` | 撮った写真をまとめて見る。ホーム・ふりかえり・写真の帯から開ける |
| `JourneySummary.walkedMeters` | 歩いた距離。**跡があれば実測、無ければ駅を結んだ目安**。どちらかを画面に明示する |

**決めたこと（外すと作り直しになる）**

- **跡はサーバーへ送らない。** プライバシーポリシー §1 で約束している。
  共有するのは「到着した駅と時刻」だけ
- **跡は歩いている間だけ残す。** 待っている間まで拾うと、駅で立ち止まった点が団子になる。
  精度が60mより粗い点も混ぜない（地下や谷間で街を横切る線になる）
- **色は距離で刻む（時間ではない）。** 地図の上で等しい長さにならないと、
  止まっている間だけ濃くなり、速いところほど色が大雑把になる。
  止まっていた区切りは色に使わない
- **刻み幅（50m）と点の間隔（10m）は組で決まる。** 片方だけ変えると、
  1区切りに2点しか入らずGPSの誤差がそのまま色に出る
- **道路沿いの経路は「取れたら使う」。** MapKit は続けて投げると絞られるので、
  一度に取るのは手前の数区間だけ。取れなくても直線で描いて進行を止めない

---

## 10. 今回の作業：予定の作り直しと、二本立て開発の統合（2026-08-11）

### 10.1 予定・設定・進行まわりの作り直し

ビルド26（導線整理）の後、`main` 側の作業ツリーで次の7点を直した。
コアの考え方は「**遊び方のルールは端末の設定ではなく、その日の予定に紐づく**」の1点に集約される。
設定に残っていた区間・サイコロ・到着判定・共有の切り替えは、すべて `Schedule` の詳細設定へ移した。
これにより「前回の設定が残っていて、今日の遊び方と違う」という食い違いが起きなくなった。

| # | 変更 | ひとことで |
|---|---|---|
| 1 | 終わった予定を自動で畳む | `Schedule.isFinished(asOf:)` が集合日の**翌日**を境目にする。消さずに `DisclosureGroup` へ畳む（`ScheduleListView`） |
| 2 | 設定を4項目に整理 | 「言語・募金・使い方とお約束・アプリ情報」だけにし、ルール類（コース・区間・サイコロ・到着判定）は予定作成へ移した（`RuleSettingsView`） |
| 3 | 予定作成を再設計 | コース→スタート/ゴール→名前→道のり→集合日時→集合場所→（折りたたみ）詳細設定、の順に並べ直した。サイコロもお題も任意にし、両方オフの「そのまま歩くだけ」も成立させた（`ScheduleDraftView`） |
| 4 | ホーム再編 | 「いまのルーム」カードを常時最上部に出し、未参加でも参加導線を出す。アクションに「ルーム」を足して4つに（`RootView`） |
| 5 | スタート駅も1駅目扱い | `GameSessionStore.startJourney()` でスタート駅の訪問を明示的に記録し、写真も残せるようにした。出目1は隣駅で止まる（着地の計算自体は変えていない） |
| 6 | お題の既定を「全員に見える」に | サーバーの `/missions` `/missions/summary` に `includeOthers` / `includeContent` を足し、既定 true にした。「共有しても当日まで見えません」という以前の不整合な文言を削った |
| 7 | 409の自動取り込み | 同時にサイコロを振って負けた側（`SyncService.push()` が409を受けた側）は、即座に `pull()` して勝った側の進行を取り込む |

### 10.2 二本立て開発が起きていた事実と、再発防止

`worktree-plan-map-share` は `59a74d9`（環状線・札幌市電・起動待ち解消のところ）で分岐し、
そこから独立に37コミット進んでいた（多言語・海外コース・地図の軌跡・写真ギャラリー・応援課金など、
§11〜§14 に書いた内容の大半）。**同じ期間に `main` 側でも d62c1e8（ルーム越権修正）と
4b79e51（ホーム再編・ビルド26）が独立に進み、両者は2026-08-11まで一度も合流していなかった。**

worktreeという仕組みそのものが悪いのではなく、**分けたまま長く放置したのが問題だった。**
`RuleSettingsView.swift` を両方が別の目的で書き換えており、言語切り替え（main側は日本語のみの
プレースホルダー、worktree側は8言語の本実装）と応援機能（main側は「募金」という未実装スタブ、
worktree側は「開発者を応援する」というStoreKit実装済みの機能）が、同じ画面の中で二重に存在していた。
マージではworktree側の実装済みのものを残し、main側の未実装スタブを外す形で解消した。

**同じことを繰り返さないために**、作業を分けるとしても短命に保ち、
`main` 側で機能追加が続くときは、こまめに `main` を取り込んで差分を小さく保つこと。
worktreeを長期の開発ラインとして使わない。

### 10.3 マージとデプロイの記録

- マージコミット: `a793fd8`（`worktree-plan-map-share` を `main` へ統合）。
  その直前の `main` 側の最新コミットは `df88dff`
- DB適用（2026-08-11 01:20〜01:25 JST、すべて成功）:
  `19_courses_v3`（阪急京都本線）・`22_courses_v4`（ベルリン／ロンドン。
  **St. James's Park と King's Cross のアポストロフィが未エスケープだったため、直してから適用**）・
  `23_courses_v5`・`24_courses_v6`・`25_migration_v5_schedule_detail`（予定の詳細設定）・
  `26_migration_v6_mission_visibility_default`（既存3ルームのお題の見え方を「いつでも見える」へ）
- コースは**14本・341駅**になった（日本7本・海外7本、内訳は §13）
- お題の見え方の既定は「いつでも見える」に仕様変更した（ユーザー要望）。
  ルームの既定・サーバーの既定・`26_migration_v6` の3か所を揃えてある
- API: 2026-08-11 01:29 JSTに統合版をApp Serviceへ再配置。
  `/health` `/health/db` `/courses`（14件）で疎通確認済み
- TestFlight: `1.0.0 (27)` を2026-08-11 01:33 JSTにアップロード成功（処理中）。
  アーカイブは `/tmp/RailAngyerApp-1.0.0-27.xcarchive`

> ⚠️ **`files/06_schema.sql`（スキーマの正）には、まだ `25_migration_v5_schedule_detail.sql` の
> 7列を書き戻していない。** v4適用時は本番反映後にベースの `CREATE TABLE` へ書き戻したので、
> v5も同様に `dbo.Schedule` の定義を更新すること（残作業）。

### 10.4 テストの状況（統合後・全通過）

| スイート | 件数 |
|---|---|
| `RailAngyerCore` | 94件 |
| `RailAngyerApi.Tests` | 64件 |
| アプリ（ユニット） | 182件 |
| アプリ（UI） | 21件 |

---

## 11. 応援課金とデータの出典（実装済み・販売は未開始）

| 対象 | 内容 |
|---|---|
| `TipJar` | StoreKit 2。**Consumable 3段（¥300 / ¥600 / ¥1000）** |
| `SupportDeveloperView` | 応援の画面。**何も解放しないことを先に書く** |
| `AttributionView` | 国土数値情報・OpenStreetMap・MapKit の出典 |
| `RailAngyerApp/StoreKit/Tips.storekit` | 手元で試すための商品定義 |

**決めたこと（外すと作り直しになる）**

- **買っても何も起こらない。** 機能を1つでも解放した瞬間、
  端末をまたぐ復元・検証・同期がついてくる
- **Consumable にする。** 復元すべき権利が無いので `Restore Purchases` の
  導線が要らない（Non-consumable にすると審査で必須になる）。何度でも応援できる
- 応援した回数は**この端末の中だけ**（`UserDefaults`）。権利ではないので消えてよい
- 導線は**設定の中だけ**。遊びの流れには一切割り込ませない
- **出典表示は義務。** OpenStreetMap は ODbL で、表示に加えて派生データにも条件がある

**まだできないこと（人手が要る）**

- App Store Connect に**商品を3つ登録する**（`com.non-migi.RailAngyerApp.tip300` など）
- **有料Appの契約**と、銀行口座・税務情報の登録
- App Store への公開（**いまは TestFlight のみ。売れない**）

> 商品が取れないあいだは、応援の画面が「いまは応援を受け取れません」と出て静かに畳む。
> **登録前はこれが正しい状態。**
>
> ⚠️ 手元で金額の並びを見るには、Xcode の **Debug > StoreKit** から
> `RailAngyerApp/StoreKit/Tips.storekit` を選ぶ。
> `project.yml` からスキームへ配線する方法は、パスが解決されず**黙って効かない**。

---

## 12. リリースの自動化・公開ページ・計測（実装済み）

| 対象 | 内容 |
|---|---|
| `tools/asc-release.py` | App Store の説明・キーワード・URL・ビルド紐づけを**APIから流し込む**。文面の正は `RailAngyerApp/AppStore/ja/*.txt` |
| `railangyer-legal`（別リポジトリ） | プライバシーポリシーと利用規約を GitHub Pages で公開 |
| `Telemetry`（TelemetryDeck） | 利用状況の測定。App ID は `3548357F-...`（2026-08-10 設定）。**テスト中は送らない** |

**APIで送れるもの / 送れないもの**

- 送れる：説明、キーワード、宣伝用テキスト、サポートURL、プライバシーURL、ビルドの紐づけ
- **送れない**：有料Appの契約、口座と税務情報、価格、スクリーンショット、
  「Appのプライバシー」の申告 — **ここは人がブラウザで入れる**

> ⚠️ **初回リリースではリリースノート（`whatsNew`）を編集できない。**
> 送ろうとすると 409 STATE_ERROR で**リクエスト全体が弾かれる**。
> `asc-release.py` は過去の版の状態を見て、初回なら自動で外す。

**計測で決めたこと（外すと約束を破る）**

- 送る：画面や操作の種類、コース名、端末の種類、アプリの版
- **送らない：位置（座標・駅名）、写真、お題の中身、表示名、招待コード**
- 駅名は「いまどこにいるか」に近いので送らない。コース名までなら
  「どの路線が遊ばれているか」しか分からず、次に足すコースを決められる
- 送る出来事は `Telemetry.swift` に**すべて並べる**。呼び出し側に文字列を散らさない

**文面は3か所そろえる**

利用規約とプライバシーポリシーは、①`files/16` `files/21`（正）
②`RailAngyerApp/Resources/*.md`（アプリ同梱）③`railangyer-legal`（公開ページ）
にある。**片方だけ直すとアプリ内と公開ページで食い違う。**

---

## 13. 日本の外のコース（完了）

全14本・341駅・6か国（日本・アメリカ・イギリス・ドイツ・ロシア・中国）になった。

| 対象 | 内容 |
|---|---|
| `berlin_ringbahn.json` | ベルリン Ringbahn 27駅・環状。直線35.4km（営業キロ37.5km） |
| `london_circle.json` | ロンドン サークル線 27駅・環状。直線20.0km（Inner Circle 約21km） |
| `moscow_koltsevaya.json` | モスクワ 環状線 12駅・環状。直線18.4km（公称19.4km）。いちばん手頃な一周 |
| `moscow_bolshaya.json` | モスクワ 大環状線 29駅・環状。直線54.0km（公称57.5km） |
| `beijing_line10.json` | 北京地下鉄10号線 45駅・環状。直線54.7km（公称57.0km） |
| `nyc_flushing.json` | ニューヨーク 7号線 22駅。直線15.9km。クイーンズ側は高架で線路沿いをそのまま歩ける |
| `la_eline.json` | ロサンゼルス Eライン 29駅。直線34.7km。ダウンタウンからサンタモニカの海まで |
| `osaka_loop.json` | 大阪環状線 19駅・環状。直線20.7km（営業キロ21.7km）。山手線の次に歩かれている環状線 |
| `files/22_courses_v4.sql`〜`files/24_courses_v6.sql` | DB投入。**2026-08-11 01:20〜01:25 JSTに本番へ適用済み**（`14_引き継ぎ.md` §10.3） |

**決めたこと**

- **並び順は路線リレーションの member 順から取る。** 方位角で並べると、
  円から外れた形（サークル線のテムズ川沿い）で順序が狂う
- サークル線は2009年から実際には「らせん」だが、**環になっている元の Inner Circle だけ**を扱う
  （ハマースミス支線の7駅を外す）
- 内回り／外回りにあたる呼び分けは**実在するものを使う**（S41／S42、モスクワ・北京も内環/外環にあたる呼び分けを使う）
- 駅間が短いロンドンは到着半径を110mにする（札幌市電の90mと同じ考え方）
- 海外の駅名は**現地の表記のまま**にする。日本語に直すと、現地で標識と突き合わせられなくなる
- マンハッタン一周（Great Saunter）は見送った。遊歩道が途切れており、座標を推測で埋めずに済む順路を組み立てられなかった

> **日本前提だったテストが3つ落ちた。** 「すべて日本の下に入る」「国コードはJP」
> 「環状は山手線と札幌市電」。**世界対応を謳うなら、日本だけを前提にした断定を残さない。**

---

## 14. 多言語対応（着手済み・未完）

ParadoxLab と同じ **8言語**（ja / en / de / es / fr / ko / ru / zh-Hans）。
正は日本語（`sourceLanguage: ja`）。

| 対象 | 状態 |
|---|---|
| `RailAngyerApp/Resources/Localizable.xcstrings` | **64語ぶん**を8言語で訳した |
| `AppLanguage` / `LanguageSetting` | **アプリの中で言語を切り替えられる**（設定＞言語） |
| 画面に出る日本語 | **229語**（`Text(` などの第1引数にある文字列） |
| 残り | **169語**。訳が無いものは**日本語のまま出る**（String Catalog の既定） |

> ⚠️ **言語の切り替えは `\.locale` の差し替えでやっている。**
> `UserDefaults` の `AppleLanguages` を書き換える手もあるが、
> **効かせるにはアプリの再起動が要る**。歩いている最中に再起動させたくない。
> `\.locale` を画面のいちばん外側（`RootView`）で流せば、選んだその場で切り替わり、
> 下のシートまで一度に変わる。
>
> ⚠️ **文字列をつなげて書いた箇所は、このままでは訳せない。**
> `Text("あれは" + "これです")` や `Text("\(n) 駅")` は
> 抜き出しの対象にならない。長い説明文（footer）にこの形が多い。
> **訳す前に、1つの文字列リテラルに直す必要がある。**
>
> 数を数える文言（「16駅」「3人」）は、言語によって複数形の規則が違う。
> String Catalog の **plural variations** を使う。単純に置き換えると英語で "1 stations" になる。

**確かめ方**（実際に効いているかはこれで見る）

```bash
xcodebuild ... -only-testing:RailAngyerAppUITests/LocalizationUITests test
```

`-AppleLanguages (en)` を渡してアプリを起動し、訳が画面に出ることを見ている。

---

## 15. 人手でしかできない作業（エージェント側では完了できない）

| 作業 | なぜ |
|---|---|
| **実地テスト**（全16駅で自動到着するか・徒歩テンポ・電池・地下駅の測位） | 実際に歩くしかない。**座標はすべて概値**なので、ここで補正する前提 |
| **実機での2台通し確認**（参加して同期する） | 自動署名アーカイブまでは完了。物理端末2台での同時操作は人手が要る |
| **TestFlight 配布開始**（= v1.0） | App Icon・起動画面・プライバシー・文面・Explicit App ID・Appレコードを用意し、`1.0.0 (1)`をApp Store Connectへアップロード済み。処理完了後の電話番号・テスター指定はアカウント画面で行う。詳細は `15_TestFlight準備.md` |
| **マネージドIDへの切り替え** | `az role assignment create`（Blobデータ権限の付与）が権限で弾かれた。コマンドは `03_Azure構成と料金.md` に記載 |

---

## 16. 秘密情報の扱い

- **接続文字列・キーはリポジトリに置かない。** ローカルは user-secrets、App Service はアプリケーション設定
- `railangyer_app` のパスワードは macOS キーチェーン
  （`security find-generic-password -a railangyer_app -s RailAngyer -w`）
- ストレージのアカウントキーは会話の中で共有された経緯がある。
  気になる場合は `az storage account keys renew -n railangyer -g RailAngyer --key key1` で失効できる
  （失効したら App Service の `ConnectionStrings__Storage` と user-secrets の両方を貼り替える）

---

## 17. 進め方の約束ごと（この作業で守ってきたこと）

- **個人開発なので `main` に直接コミットする。** ブランチは切らない
- コミットメッセージは日本語。**何をしたかより、なぜそうしたかを書く**
- 実装したら**必ずテストを足し、全スイートを流してからコミットする**
- ドキュメント（`files/`）はコードと同じコミットで直す。**実態とズレた記述を残さない**
- 「動いた」と言う前に、実際に流した結果を確認する。
  API は実物のAzureに対しても1往復させて確かめてきた（確認用に作ったデータは毎回消している）

---

## 18. ODbL 帰属表示の是正と、NY・LA 座標の出典確定（2026-08-15）

出典画面（`AttributionView`）の OpenStreetMap 欄が「阪急京都本線と札幌市電」のままで、
あとから足した海外コースが漏れていた。**ODbL の帰属表示義務に反する状態**だったので是正した。

**やったこと**

- `AttributionView` の ODbL 欄に **OSM 由来の全10コース**を列挙した：
  阪急京都本線・札幌市電・大阪環状線・ベルリン Ringbahn・ロンドン サークル線・
  モスクワ 環状線・モスクワ 大環状線・北京地下鉄10号線・ニューヨーク 7号線・ロサンゼルス Eライン
- `StationMaster.swift` の各コースの doc コメントに、SQL 側と同じ
  「座標と並び順は OpenStreetMap の路線リレーション（© OpenStreetMap contributors / ODbL）」を明記した

**ニューヨーク 7号線・ロサンゼルス Eライン・大阪環状線の座標の出典確定**

コード側に出典コメントが無かったため、2026-08-15 に
Overpass API（`https://overpass-api.de/api/interpreter`）から路線リレーションの
停車ノード（`node(r:"stop")`）を取得し、同梱 JSON の座標と突き合わせた。

| コース | OSM リレーション | 結果 |
|---|---|---|
| ニューヨーク 7号線 | relation **9699137**（34th St–Hudson Yards → Flushing–Main St） | 22駅すべて **0.1m 以内で一致** |
| ロサンゼルス Eライン | relation **6584633**（Downtown Santa Monica → Atlantic） | 29駅すべて **0.1m 以内で一致** |
| 大阪環状線 | relation **10073682**（JR大阪環状線 外回り） | 19駅すべて **0.1m 以内で一致** |

事実上の完全一致なので、**座標は差し替えず、既存の座標を OSM 由来として正式化した**
（`24_courses_v6.sql` の冒頭コメントどおり。同ファイル末尾にも照合の記録を追記）。
JSON にはコメントが書けないため、出典は `StationMaster.swift` の doc コメントと SQL 側に残す。

> ⚠️ **コースを足したら `AttributionView` の列挙も同じコミットで更新する。**
> 今回の漏れは「SQL にだけ出典を書いて、画面を直し忘れた」ことで起きた。

---

## 19. 法的リスクへの対応（2026-08-15）

App Store ガイドライン1.2（UGC）と、日本法・GDPR まわりの穴を潰した回。

### 決めたこと（迷ったら、ここに戻る）

| 論点 | 決めたこと | 理由 |
|---|---|---|
| 除名は相手のデータを消すか | **消す（破壊的なまま）** | 投稿者本人以外は個別に消せない実装なので、**除名が「共有された不適切な投稿を消せる唯一の手段」**。非破壊にすると、荒らしを追い出せても投稿が残る。被害は有界で、消えるのはサーバー側の複製だけ（写真の実体は各端末に残り、取り込みは足すだけ） |
| 作成者が抜けたルーム | **残った最古参へ引き継ぐ**（`JoinedAt` 最小、同着は `MemberId`）。空になったルームへ入り直した人は作成者になる | 以前は `CreatedBy` を null にしていたため、**誰も除名できず、その人が立てた予定も誰も直せない**（全員403）状態になっていた |
| ガイドライン1.2 の要件 | ブロック・通報・連絡先で満たす。**除名は上乗せ**であり、無いルームがあってもリジェクト事由ではない | 1.2 が求めるのは「利用者が自分を守れること」であって「管理者が他人を排除できること」ではない |
| 特商法の表記 | 課金画面から**1タップ**で、**その表記だけの画面**（`Commerce.md`）へ。**言語を切り替えても常に日本語** | 規約全文の§11に埋もれていると「明瞭に表示」に欠ける。日本の消費者を守る日本法なので、表示言語を英語にした日本の利用者が読めないのは避ける |
| 文書に書いてよいこと | **実装にある機能だけ**。無い機能は書かない | 今回の発端が「規約に書いてある削除手段が実装に無い」だった。文書が先走ると、審査で実装との食い違いを突かれる |

### 文書は3系統ある（片方だけ直さない）

`Resources/*.md`（アプリ同梱）＝ `files/16` `files/21` `files/28`（正）、`files/privacy-policy.html`（公開）。
英語版 `.en.md` も同じ改定を反映する。**ただし `Commerce.md` に英語版は作らない**（上表の理由）。

### 退室と削除の設計

抜ける操作は**待たせない**（即 dismiss）。削除に失敗したら、やり直すための控え（ルームIDとトークン）を
キーチェーンに置き、起動時とルーム画面を開いたときに自動で送り直す。5回または7日で打ち切り、
そのときだけ「メールで削除を依頼してください」と伝える。送信口は `sendPendingLeave()` **1つに集約**し、
番人（`isSendingLeave`）で二重送信を防ぐ。**この控えの存在はプライバシーポリシー §7 に書いてある。**

---

## 20. 既知の問題（直していない。踏んだときのために）

### 効果のあるお題では、内訳の「移動」と「お題」が同じ時間を二重に数える

`WalkTiming.breakdown` は、お題の時間を **`[着地, 最初の効果訪問]`** で測る。
一方で効果移動の区間も **`[着地, 効果訪問]`** ——**まったく同じ窓**を使っている。
そのため「進む／戻る／指定駅へ」のお題を引いたターンでは、以前から
**「移動＋お題」が旅の長さを超えうる**（超えたぶんは「その他」が `max(0,…)` で潰れて表に出ない）。
この窓に休憩が重なると、歩きとお題の両方から引かれて**休憩も二重に控除される**。

**直すなら「お題が終わった時刻」を記録するところから。** いまはその境目を持っていないので、
着地から効果移動までを両者で共有している。ただし
**「あとで」（`deferMission`）で判定を保留できるようになったため、お題の判定は何時間も後になりうる**。
つまり「お題に費やした時間」の定義そのものを決め直す話で、片手間には直せない。

現地で踏む頻度は低い（効果で歩いている最中に休むと起きる）。合計一致のテスト
（`RestPeriodTests`）は**効果の無いターンで**固定してあるので、直すときはそこも広げること。

### 時間とペースの単位が、9言語すべてで日本語のまま

`RailAngyerCore/Sources/RailAngyerCore/Pace.swift` の `DurationText.text`（:99-107）と
`Pace` の表記が **`秒` / `分` / `時間` を文字列に直書き**している。
`RailAngyerCore` は多言語化の対象に入っていない（`Localizable.xcstrings` はアプリ側にしか無い）ため、
**英語で開いても「Total 32秒」「Avg 0分4秒/km」と出る**。9言語ぶんの
スクリーンショットを撮って初めて気づいた。

直すなら `DateComponentsFormatter`（単位を勝手に訳してくれる）に寄せるのが筋。
ただし `RailAngyerCore` は端末の言語しか知らず、**アプリ内で選んだ言語**（`AppLanguage`）は
見えないので、`DurationText.locale` のような差し込み口を1つ作って、
アプリ側が言語を変えたときに入れる形になる。
既存テストが「45秒」「1時間5分」を期待しているので、そこも直すこと。
